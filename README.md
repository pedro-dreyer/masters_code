## Run experiments

The experiments scripts are located in `train_scripts` folder, edit/change the `train.sh` file in the root folder for the desired experiment and execute it:

`./train.sh`

Results are saved in the `results` folder
